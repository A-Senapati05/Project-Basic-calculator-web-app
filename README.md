# 🧮 Calculator Web App

A simple and responsive calculator built using **HTML, CSS, and JavaScript**.  
This project performs basic arithmetic operations and demonstrates DOM manipulation, event handling, and clean UI design.

---

## 🚀 Features

- Basic arithmetic operations:  
  **Addition (+), Subtraction (−), Multiplication (×), Division (÷)**
- Percentage (%) calculation
- Decimal point support
- Clear all (**AC**) button
- Backspace (**⌫**) button
- Error handling for invalid expressions
- Responsive and modern UI

---

## 🛠️ Technologies Used

- **HTML5** – Structure of the calculator  
- **CSS3** – Styling and layout (Grid-based design)  
- **JavaScript (ES6)** – Calculator logic and event handling  

---

## ⚙️ How It Works

1. Each button listens for a click event.
2. The button value is read using `textContent`.
3. User input is stored in a variable called `expression`.
4. Calculator symbols (×, ÷, −, %) are converted into JavaScript-readable operators.
5. The expression is evaluated and displayed on the screen.

---

## ▶️ How to Run

1. Clone the repository:
   ```bash
   git clone https://github.com/your-username/your-repo-name.git

